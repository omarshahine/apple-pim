{
  "id": "apple-pim-cli",
  "name": "Apple PIM: Calendar, Reminders, Contacts, Mail",
  "description": "macOS-only. Wraps four native Swift CLIs (calendar-cli, reminder-cli, contacts-cli, mail-cli) you build locally from source via ./setup.sh \u2014 no binaries are downloaded by the registry. Grants the agent read/write access to Calendar, Reminders, Contacts, and Mail.app (including send/delete) once you approve the corresponding macOS TCC and Automation prompts.",
  "version": "3.9.1",
  "configSchema": {
    "type": "object",
    "properties": {
      "binDir": {
        "type": "string",
        "description": "Path to Swift CLI binaries directory"
      },
      "profile": {
        "type": "string",
        "description": "Default profile name for Apple PIM (Personal Information Manager) config"
      },
      "configDir": {
        "type": "string",
        "description": "Default config directory for Apple PIM (Personal Information Manager)"
      },
      "mailAttachmentsConfig": {
        "type": "string",
        "description": "Path to mail attachment policy JSON. Default: ~/.config/apple-pim/mail-attachments.json. Mail send/reply attachments are default-denied unless this file opts in with allowedRoots."
      }
    },
    "additionalProperties": false
  },
  "uiHints": {
    "binDir": {
      "label": "CLI Binary Directory",
      "help": "Path to the directory containing calendar-cli, reminder-cli, contacts-cli, mail-cli. Auto-detected from PATH if not set.",
      "placeholder": "~/.local/bin"
    },
    "profile": {
      "label": "Profile",
      "help": "Config profile name for filtering calendars/lists/contacts. See ~/.config/apple-pim/profiles/."
    },
    "configDir": {
      "label": "Config Directory",
      "help": "Override the config root directory for Apple PIM (default: ~/.config/apple-pim/).",
      "placeholder": "~/.config/apple-pim"
    },
    "mailAttachmentsConfig": {
      "label": "Mail Attachments Policy File",
      "help": "Mail send/reply attachments are default-denied. To allow attachments, point this at a JSON file with {\"enabled\": true, \"allowedRoots\": [\"~/Downloads\"]}. Even when enabled, paths in ~/.ssh, ~/.aws, etc. and files like id_rsa, *.pem, *secret* are always refused.",
      "placeholder": "~/.config/apple-pim/mail-attachments.json",
      "advanced": true
    }
  },
  "contracts": {
    "tools": [
      "apple_pim_calendar",
      "apple_pim_reminder",
      "apple_pim_contact",
      "apple_pim_mail",
      "apple_pim_system"
    ]
  }
}
