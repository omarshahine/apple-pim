---
import FeatureCard from './FeatureCard.astro';
---

<section id="features" class="py-24 px-4 sm:px-6 lg:px-8">
  <div class="max-w-6xl mx-auto">
    <!-- Section heading -->
    <h2 class="text-3xl sm:text-4xl font-bold text-center mb-4 text-foreground">Everything You Need</h2>
    <p class="text-muted text-center mb-16 max-w-2xl mx-auto">
      Native macOS PIM integration with 5 MCP tools covering Calendar, Reminders, Contacts, Mail, and system management.
    </p>

    <!-- Feature grid: 3 columns, symmetric layout -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      <FeatureCard
        icon="📅"
        title="Calendar Management"
        description="List calendars, create/read/update/delete events, search by date or title, batch create, and full recurrence rule support."
      />
      <FeatureCard
        icon="✅"
        title="Reminder Management"
        description="List reminder lists, create/complete/update/delete reminders, smart filters (overdue, today, week), and batch operations."
      />
      <FeatureCard
        icon="👤"
        title="Contact Management"
        description="List groups, create/read/update/delete contacts, search by name/email/phone, birthday and relationship support."
      />
      <FeatureCard
        icon="📧"
        title="Mail Integration"
        description="List accounts and mailboxes, read/search/move/delete messages, update flags — all through Mail.app and JXA."
      />
      <FeatureCard
        icon="🔒"
        title="Per-Domain Control"
        description="Enable or disable entire domains independently, allowlist/blocklist calendars and reminder lists. Privacy-first design."
      />
      <FeatureCard
        icon="🤖"
        title="Multi-Agent Isolation"
        description="Per-call config and profile overrides, workspace isolation. Give each agent different PIM access for safe delegation."
      />
    </div>
  </div>
</section>
